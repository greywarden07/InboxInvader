# 🎨 Deployment Architecture Diagram

## Current Setup (Local Development)

```
┌─────────────────────────────────────────────────────────┐
│                    Your Computer                        │
│                                                          │
│  ┌──────────────────┐         ┌──────────────────┐     │
│  │   Frontend       │         │    Backend       │     │
│  │   Next.js        │ ◄─────► │    Flask         │     │
│  │   Port: 3000     │  HTTP   │    Port: 5000    │     │
│  │   localhost:3000 │         │  localhost:5000  │     │
│  └──────────────────┘         └────────┬─────────┘     │
│                                         │               │
│                                    ┌────▼────┐          │
│                                    │ SQLite  │          │
│                                    │   DB    │          │
│                                    └─────────┘          │
└─────────────────────────────────────────────────────────┘
```

**Problem:** Only accessible from your computer!

---

## Production Setup (After Deployment)

```
┌─────────────────────────────────────────────────────────────────┐
│                          Internet                               │
│                                                                  │
│  ┌───────────────────────────────────────────────────┐          │
│  │                  User's Browser                   │          │
│  │           https://your-app.vercel.app             │          │
│  └───────────────────────┬───────────────────────────┘          │
│                          │                                       │
│                          │ HTTPS                                 │
│                          ▼                                       │
│  ┌─────────────────────────────────────────────────┐            │
│  │              Vercel (Frontend)                  │            │
│  │  ┌──────────────────────────────────────┐       │            │
│  │  │    Next.js Application               │       │            │
│  │  │    - Login/Signup UI                 │       │            │
│  │  │    - Email Sender Form               │       │            │
│  │  │    - Template Manager                │       │            │
│  │  │    - Dark Mode                       │       │            │
│  │  └────────────────┬─────────────────────┘       │            │
│  └───────────────────┼─────────────────────────────┘            │
│                      │                                           │
│              API_URL │ (NEXT_PUBLIC_API_URL)                     │
│                      │ https://backend.onrender.com              │
│                      ▼                                           │
│  ┌──────────────────────────────────────────────────┐           │
│  │              Render (Backend)                    │           │
│  │  ┌───────────────────────────────────────┐       │           │
│  │  │    Flask Application                  │       │           │
│  │  │    - /login, /signup                  │       │           │
│  │  │    - /send, /templates                │       │           │
│  │  │    - /export-csv                      │       │           │
│  │  │    - JWT Authentication               │       │           │
│  │  └─────────────────┬─────────────────────┘       │           │
│  │                    │                             │           │
│  │              ┌─────▼──────┐                      │           │
│  │              │   SQLite   │                      │           │
│  │              │  Database  │                      │           │
│  │              │ (Users &   │                      │           │
│  │              │ Templates) │                      │           │
│  │              └────────────┘                      │           │
│  └──────────────────────────────────────────────────┘           │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

**Benefits:** 
- ✅ Accessible from anywhere
- ✅ HTTPS/SSL included (secure)
- ✅ Auto-deploys on git push
- ✅ Free hosting!

---

## Data Flow Example: User Sends Email

```
Step 1: User Fills Form
┌──────────────┐
│ User Browser │
│ (Vercel URL) │
└──────┬───────┘
       │
       │ User enters: email, password, recipients, subject, body
       │
       ▼
┌──────────────┐
│ React State  │ (stores form data locally)
└──────┬───────┘
       │
       │ User clicks "Send Emails"
       │
       ▼

Step 2: Frontend Calls Backend API
┌──────────────────────────────────────┐
│ axios.post(                          │
│   `${API_URL}/send`,                 │
│   { email, password, recipients...}, │
│   { headers: { Authorization: ... }} │
│ )                                    │
└──────┬───────────────────────────────┘
       │
       │ HTTP POST Request
       │ https://backend.onrender.com/send
       │ Headers: Authorization: Bearer <token>
       │ Body: { smtp_server, email, password, recipients[], subject, body }
       │
       ▼

Step 3: Backend Processes Request
┌─────────────────────────────────────┐
│ Flask Backend on Render             │
│                                     │
│ 1. Verify JWT token                │
│ 2. Extract user_id from token      │
│ 3. Validate SMTP credentials        │
│ 4. Loop through each recipient:     │
│    - Connect to SMTP                │
│    - Send email                     │
│    - Record success/failure         │
│    - Wait (delay_seconds)           │
│ 5. Return array of results          │
└──────┬──────────────────────────────┘
       │
       │ HTTP 200 Response
       │ { success: true, results: [...] }
       │
       ▼

Step 4: Frontend Displays Results
┌──────────────────────────────────────┐
│ React Updates State:                 │
│ - setResults(response.data.results)  │
│ - setLoading(false)                  │
│                                      │
│ User sees table:                     │
│ ┌─────────────────────────────────┐ │
│ │ Email          | Status         │ │
│ │ test@email.com | ✅ Sent        │ │
│ │ bad@email.com  | ❌ Failed      │ │
│ └─────────────────────────────────┘ │
└──────────────────────────────────────┘
```

---

## Environment Variable Flow

### Development (localhost)

```
web/.env.local:
  NEXT_PUBLIC_API_URL=http://localhost:5000
       │
       │ Next.js reads on build
       │
       ▼
Frontend Code:
  const API_URL = process.env.NEXT_PUBLIC_API_URL
  // API_URL = "http://localhost:5000"
       │
       │ API calls go to local backend
       │
       ▼
Backend:
  app.run(port=5000)
  // Runs on localhost:5000
```

### Production (deployed)

```
Vercel Environment Variable:
  NEXT_PUBLIC_API_URL=https://backend.onrender.com
       │
       │ Vercel injects during build
       │
       ▼
Frontend Code:
  const API_URL = process.env.NEXT_PUBLIC_API_URL
  // API_URL = "https://backend.onrender.com"
       │
       │ API calls go to production backend
       │
       ▼
Render Environment Variable:
  PORT=10000 (set by Render)
  SECRET_KEY=abc123... (you set this)
  ALLOWED_ORIGINS=https://your-app.vercel.app
       │
       │ Flask reads on startup
       │
       ▼
Backend:
  port = int(os.environ.get('PORT', 5000))
  app.run(port=port)
  // Runs on Render's assigned port
```

---

## Why Two Separate Hosts?

### ❌ Why Not Just Vercel?

```
┌────────────────────────────────┐
│         Vercel                 │
│  ┌──────────────────────┐      │
│  │  Next.js Frontend ✅ │      │
│  └──────────────────────┘      │
│  ┌──────────────────────┐      │
│  │  Flask Backend ❌    │      │  ← Vercel doesn't support Python backends!
│  └──────────────────────┘      │
└────────────────────────────────┘
```

**Vercel Limitations:**
- ❌ No long-running processes (serverless functions only)
- ❌ No persistent filesystem for SQLite
- ❌ Python support limited to serverless functions
- ❌ Functions have 10-60 second timeout

**Our Backend Needs:**
- ✅ Long-running SMTP connections
- ✅ Persistent SQLite database file
- ✅ Full Flask application
- ✅ No timeout limits

### ✅ Solution: Split Architecture

```
┌────────────────────────┐      ┌────────────────────────┐
│       Vercel           │      │       Render           │
│  ┌──────────────┐      │      │  ┌──────────────┐      │
│  │  Next.js ✅  │ ◄────┼──────┼─►│  Flask ✅    │      │
│  └──────────────┘      │ API  │  └──────┬───────┘      │
│                        │      │         │              │
│  Static files,         │      │    ┌────▼─────┐        │
│  React UI,             │      │    │ SQLite   │        │
│  Client-side code      │      │    │ Database │        │
│                        │      │    └──────────┘        │
└────────────────────────┘      └────────────────────────┘

    Free tier: ✅               Free tier: ✅
    Best for frontend           Best for backend
```

---

## Deployment Timeline

```
Initial State (before any deployment):
┌──────────────────────────────────────┐
│ Code on your computer only           │
│ Backend: localhost:5000              │
│ Frontend: localhost:3000             │
│ Database: local SQLite file          │
└──────────────────────────────────────┘


After Step 1 - Deploy Backend to Render:
┌──────────────────────────────────────┐
│ Backend: ✅ https://backend.onrender.com
│ Frontend: ❌ still on localhost:3000  │
│ Database: ✅ on Render server         │
└──────────────────────────────────────┘


After Step 2 - Update Frontend Config:
┌──────────────────────────────────────┐
│ Code updated with Render backend URL │
│ Committed and pushed to GitHub       │
└──────────────────────────────────────┘


After Step 3 - Deploy Frontend to Vercel:
┌──────────────────────────────────────┐
│ Backend: ✅ https://backend.onrender.com
│ Frontend: ✅ https://app.vercel.app    │
│ Database: ✅ on Render server         │
└──────────────────────────────────────┘


After Step 4 - Update CORS:
┌──────────────────────────────────────┐
│ Backend allows requests from Vercel  │
│ Frontend can call Backend APIs       │
│ ✅ FULLY FUNCTIONAL!                  │
└──────────────────────────────────────┘
```

---

## Cost Structure

### Free Tier Limits

```
┌─────────────────────────────────────────────────────┐
│                    Vercel Free                      │
│  - 100GB Bandwidth/month                            │
│  - Unlimited deployments                            │
│  - Automatic HTTPS                                  │
│  - Custom domains (1)                               │
│  - Perfect for: Static sites, Next.js apps          │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│                    Render Free                      │
│  - 750 hours/month (enough for 1 app)               │
│  - Spins down after 15 min inactivity               │
│  - 512MB RAM                                        │
│  - Automatic HTTPS                                  │
│  - Wake time: ~30 seconds                           │
│  - Perfect for: APIs, small backends                │
└─────────────────────────────────────────────────────┘

Total Cost: $0/month (with some limitations)
```

### Paid Options

```
┌─────────────────────────────────────────────────────┐
│              Render Starter - $7/month              │
│  - Always-on (no spin down)                         │
│  - Instant wake time                                │
│  - Better for: Production apps with regular traffic │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│              Vercel Pro - $20/month                 │
│  - More bandwidth (1TB)                             │
│  - Team features                                    │
│  - Analytics                                        │
│  - Better for: Professional projects, teams         │
└─────────────────────────────────────────────────────┘
```

---

## Security Architecture

```
┌────────────────────────────────────────────────────────┐
│                   Security Layers                      │
│                                                         │
│  Layer 1: HTTPS/SSL (Automatic)                        │
│  ┌─────────────────────────────────────────────┐       │
│  │ All traffic encrypted in transit            │       │
│  │ Certificates managed by Vercel/Render       │       │
│  └─────────────────────────────────────────────┘       │
│                                                         │
│  Layer 2: CORS (Backend Configuration)                 │
│  ┌─────────────────────────────────────────────┐       │
│  │ Only your Vercel domain can call APIs       │       │
│  │ Other domains blocked by browser            │       │
│  └─────────────────────────────────────────────┘       │
│                                                         │
│  Layer 3: JWT Authentication                           │
│  ┌─────────────────────────────────────────────┐       │
│  │ Login required to access APIs               │       │
│  │ Token expires after 24 hours                │       │
│  │ Token signed with SECRET_KEY                │       │
│  └─────────────────────────────────────────────┘       │
│                                                         │
│  Layer 4: Password Hashing                             │
│  ┌─────────────────────────────────────────────┐       │
│  │ Passwords never stored in plain text        │       │
│  │ Werkzeug PBKDF2 hashing                     │       │
│  └─────────────────────────────────────────────┘       │
│                                                         │
│  Layer 5: Per-User Data Isolation                      │
│  ┌─────────────────────────────────────────────┐       │
│  │ Templates filtered by user_id               │       │
│  │ Users can only see their own data           │       │
│  └─────────────────────────────────────────────┘       │
└────────────────────────────────────────────────────────┘
```

---

## Troubleshooting Diagram

```
Problem: Can't login on production

Check 1: Is backend running?
  curl https://backend.onrender.com/health
    ↓ NO (error or timeout)
    └─► Check Render logs, restart service
    ↓ YES (returns {"status": "healthy"})
    
Check 2: Is CORS configured?
  Browser console shows CORS error?
    ↓ YES
    └─► Update ALLOWED_ORIGINS on Render
    ↓ NO
    
Check 3: Is API_URL correct?
  Frontend calling right URL?
    ↓ NO (still using localhost)
    └─► Update NEXT_PUBLIC_API_URL in Vercel
    ↓ YES
    
Check 4: Is authentication working?
  Login returns 401 Unauthorized?
    ↓ YES
    └─► Check username/password, database
    ↓ NO (200 OK)
    
✅ Should be working!
```

---

## Quick Reference: Important URLs

```
Development:
  Frontend: http://localhost:3000
  Backend:  http://localhost:5000
  Database: backend/inboxinvader.db (local file)

Production:
  Frontend: https://your-app.vercel.app
  Backend:  https://your-backend.onrender.com
  Database: On Render server (not directly accessible)

Configuration:
  Vercel Dashboard:  https://vercel.com/dashboard
  Render Dashboard:  https://dashboard.render.com
  GitHub Repo:       https://github.com/greywarden07/InboxInvader

Logs:
  Vercel:  Dashboard → Project → Deployments → [Latest] → Logs
  Render:  Dashboard → Service → Logs tab
```

---

Made with ❤️ for easy understanding of deployment architecture!
