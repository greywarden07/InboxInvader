# Frontend (Next.js)

This folder contains a small Next.js frontend that talks to the Flask backend at `http://localhost:5000`.

Run locally (Windows PowerShell):

```powershell
cd web
npm install
npm run dev
```

Open http://localhost:3000 in your browser.

If you change the backend port or host, update the URL in `web/pages/index.js`.
